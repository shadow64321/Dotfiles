# Dotfiles

# Nvim
run: git clone --depth 1 https://github.com/wbthomason/packer.nvim\
 ~/.local/share/nvim/site/pack/packer/start/packer.nvim
in the .config directory
then go to the lua/treehug directory and go into the packer to lua file and type
':so'
to source the file
then type 
':PackerSync'
and when thats done type
':Mason'
and select all the programming extensions you want

